[project]
name = "mtg-pack-opener-backend"
version = "0.1.0"
description = "MTG pack opener backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.8.0"
]